# AI Note - Obsidian 智能笔记管理插件 - 产品需求文档 (v0.5)

## 需求概述

将 AI Note 迁移到 Obsidian 插件平台,为 Obsidian 用户提供智能的笔记管理能力。提供智能归档、每日复盘、主题调研等核心功能,深度集成 Obsidian 生态系统。

### 核心变更

- **存储位置**：所有数据存储在 `.obsidian/plugins/ai-note/data/`
- **Obsidian API 集成**：使用 TFile、Vault、MetadataCache 等 Obsidian API
- **链接和嵌入支持**：生成的报告支持 Obsidian 双向链接和嵌入

### 核心设计原则

- **插件数据隔离**：所有数据存储在插件数据目录,不污染用户笔记
- **Markdown 友好**：生成的所有报告和摘要都采用 Markdown 格式
- **充分利用 Obsidian API**：使用原生能力而非自定义实现
- **摘要作为共享基础**：摘要生成是归档和调研的前置共享能力，总是先执行，由缓存机制保证效率

---

## 目录结构

```
.obsidian/plugins/ai-note/
├── data/                           # 插件数据目录
│   └── cache/                     # 缓存数据
│       ├── summaries/             # 笔记摘要
│       │   └── {noteId}.json
│       ├── folder_summaries/      # 文件夹摘要
│       │   └── {folderId}.json
│       ├── snapshots/             # 笔记快照
│       │   ├── {hash}.gz
│       │   └── snapshot-index.json
│       ├── identity/              # 用户身份档案
│       │   └── profile.json
│       ├── research/              # 主题调研缓存
│       │   ├── topics/            # 候选主题
│       │   │   └── {date}.json
│       │   └── history/           # 调研历史
│       │       └── index.json
├── main.ts                       # 插件入口
├── manifest.json                 # 插件清单
└── src/                          # 源代码目录

{vaultPath}/                       # 笔记根目录
├── 待整理/                        # 不确定归档位置的笔记（可配置）
├── 复盘/                          # 复盘报告目录（可配置）
│   ├── daily/                     # 每日复盘
│   │   └── {date}.md
│   └── weekly/                    # 每周复盘
│       └── {week}.md
└── 调研/                          # 调研报告目录（可配置）
    └── {date}-{slug}.md
```

---

## 数据存储设计

### 笔记摘要 (data/cache/summaries/{noteId}.json)

```json
{
  "path": "path/to/note.md",
  "title": "笔记标题",
  "summary": "笔记内容摘要",
  "keywords": ["关键词1", "关键词2", "关键词3"],
  "questions": ["问题1", "问题2"],
  "lastUpdated": "2025-01-15T10:00:00.000Z",
  "fileSize": 1024
}
```

### 文件夹摘要 (data/cache/folder_summaries/{folderId}.json)

```json
{
  "path": "path/to/folder",
  "theme": "文件夹核心主题",
  "keywords": ["关键词1", "关键词2", "关键词3"],
  "lastUpdated": "2025-01-15T10:00:00.000Z",
  "sampleNotes": ["笔记1.md", "笔记2.md", "笔记3.md"]
}
```

**注意**：`sampleNotes` 数组最大限制为 10 个文件，增量更新时保持最新的 10 个示例。

### 快照索引 (data/cache/snapshots/snapshot-index.json)

```json
{
  "lastSnapshotTime": "2025-01-15T22:00:00.000Z",
  "files": {
    "path/to/note.md": {
      "hash": "abc123",
      "snapshotFile": "snapshots/abc123.gz",
      "modifiedTime": 1704500000000
    }
  }
}
```

**注意**：快照使用 zlib 压缩,每个快照文件 `{hash}.gz` 存储完整笔记内容。

### 用户身份档案 (data/cache/identity/profile.json)

```json
{
  "primaryRole": "knowledge-worker",
  "secondaryRole": "researcher",
  "currentFocus": ["AI应用", "笔记管理", "Obsidian插件"],
  "vaultComposition": {
    "hasNotes": true,
    "hasCode": false,
    "hasAssets": true,
    "noteRatio": 0.8,
    "codeRatio": 0.0,
    "assetRatio": 0.2
  },
  "primaryPurpose": "knowledge-base",
  "contentAreas": [
    {
      "path": "/",
      "type": "notes",
      "theme": "个人知识管理"
    },
    {
      "path": "技术文档",
      "type": "notes",
      "theme": "技术研究"
    }
  ],
  "lastAnalyzedAt": "2025-01-15T10:00:00.000Z",
  "analysisVersion": 2
}
```

### 候选主题 (data/research/topics/{date}.json)

```json
{
  "date": "2025-01-08",
  "candidates": [
    {
      "id": "topic-001",
      "title": "VSCode Extension API 中的 FileSystemWatcher 最佳实践",
      "type": "trending",
      "source": "out/code-master/ai-note/src/services/fileScanner.ts",
      "sourceType": "code",
      "confidence": 0.85,
      "keywords": ["vscode", "extension", "filesystem"],
      "reason": "用户正在开发文件监控相关功能,可深入了解 VSCode 最新的文件系统 API"
    },
    {
      "id": "topic-002",
      "title": "如何设计高效的文件去重算法",
      "type": "problem-solving",
      "source": "prd/v0.3.md",
      "sourceType": "note",
      "confidence": 0.9,
      "keywords": ["去重", "算法", "哈希"],
      "reason": "用户在 PRD 中提到知识点级去重,可能需要具体的实现思路"
    }
  ],
  "selectedTopics": ["topic-001", "topic-002", "topic-003"]
}
```

### 调研历史索引 (data/research/history/index.json)

```json
{
  "topics": [
    {
      "id": "2025-01-07-vscode-extension-testing",
      "title": "VSCode Extension 单元测试完全指南",
      "type": "deep-dive",
      "date": "2025-01-07",
      "reportPath": "reports/2025-01-07-vscode-extension-testing.md",
      "keyPoints": [
        "VSCode Extension Test Runner 配置",
        "@vscode/test-electron 使用方法",
        "Mock vscode API 的技巧",
        "集成测试 vs 单元测试的边界",
        "CI/CD 中运行测试的注意事项"
      ],
      "relatedKeywords": ["vscode", "testing", "extension", "jest", "mocha"]
    }
  ],
  "lastCleanupAt": "2025-01-08T00:00:00.000Z"
}
```

---

## 核心功能

### 0. 摘要基础能力 (共享服务)

**核心原则**：
- **自动触发**：归档和调研功能总是先执行摘要生成，无需用户手动触发
- **缓存优化**：摘要服务内部处理缓存，只对新/变更文件调用 AI
- **设计理念**："Always summarize, let cache optimize" - 总是执行摘要，让缓存保证效率

**摘要缓存机制**：

```typescript
// 摘要文件结构
{
  "path": "path/to/note.md",
  "title": "笔记标题",
  "summary": "笔记内容摘要",
  "keywords": ["关键词1", "关键词2"],
  "contentHash": "abc123",  // 内容哈希，用于检测变更
  "lastUpdated": "2025-01-15T10:00:00.000Z"
}
```

**缓存逻辑**：
1. 读取摘要缓存文件
2. 计算当前笔记内容的哈希值
3. 对比 `contentHash`：
   - 相同 → 跳过，使用缓存
   - 不同 → 调用 AI 重新生成摘要
4. 保存更新后的摘要

**执行流程**：

```
归档/调研功能触发
    ↓
┌───────────────────────────────┐
│ 统一入口：执行摘要生成        │
└───────────────────────────────┘
    ↓
- 扫描所有笔记文件
- 检查摘要缓存
- 只对新增/变更文件调用 AI
- 更新缓存
    ↓
返回摘要统计：
{ new_summaries: N, updated_summaries: M, failed: 0 }
    ↓
继续执行归档/调研流程
```

**优势**：
- ✅ 用户体验好：无需手动"先归档再调研"
- ✅ 代码简洁：无需条件判断逻辑
- ✅ 性能优化：缓存避免重复 AI 调用
- ✅ 数据新鲜：每次都确保使用最新摘要

---

### 1. 智能归档

**核心规则**：
- **归档范围限制**：只处理根目录的笔记，已在子文件夹中的笔记不参与归档
- **上下文增强**：归档分类时,对于内容较少的笔记,强制提取文件名和父目录名作为补充参数
- **隐藏目录过滤**：自动过滤以 `.` 开头的目录（如 `.obsidian/`, `.git/`），其他目录可通过配置排除
- **根目录专注**：分类时只扫描根目录的笔记，已归档到子目录的笔记不重复处理
- **待整理重试**：移入"待整理目录"的笔记会在下次归档时重新尝试分类

**归档流程**：

```
用户触发归档 (命令/定时)
    ↓
┌───────────────────────────────┐
│ 阶段0:生成笔记摘要（共享）     │
└───────────────────────────────┘
    ↓
- 调用 SummarizerService.run()
- 检查摘要缓存（contentHash）
- 只对新/变更文件调用 AI
- 返回: { new_summaries, updated_summaries, failed }
    ↓
┌───────────────────────────────┐
│ 阶段1:生成文件夹摘要          │
└───────────────────────────────┘
    ↓
- 调用 SummarizerService.generateFolderSummaries()
- 基于笔记摘要生成文件夹主题摘要
- 存储到 data/cache/folder_summaries/{hash}.json
    ↓
┌───────────────────────────────┐
│ 阶段2:AI分类和归档            │
└───────────────────────────────┘
    ↓
- 读取 data/cache/folder_summaries/ 中的所有摘要
- 只扫描根目录笔记(不递归子目录)
- 跳过隐藏目录和非文本文件
- AI 批量分类(按10个一批)
- 解析结果:
  - confidence < 0.7 或 uncertain → 自动移至待整理目录（无用户交互）
  - 其他 → 高置信度,自动归档
    ↓
┌───────────────────────────────┐
│ 阶段3:执行笔记移动            │
└───────────────────────────────┘
    ↓
- 高置信度文件：使用 Vault.rename() 移动笔记到目标文件夹
- 不确定文件：自动移至待整理目录
- 创建目标目录(如不存在)
- 处理文件名冲突（自动添加 .1, .2 后缀）
- 更新摘要中的文件路径
- 增量更新文件夹摘要（追加新文件名到 sampleNumbers，取最后 10 个）
- Status Bar 显示移动进度（如"移至待整理: 3/5"）
    ↓
完成
```

**待整理目录重试机制**：
- 移入待整理目录的笔记会在下次归档时自动重新尝试分类
- 无需用户手动干预，系统会持续在后台优化分类结果
- 待整理目录中的文件会与根目录文件一起参与分类流程
```

---

### 2. 每日/每周复盘

**核心特性**：
- **文件快照**：压缩存储笔记内容,精确追踪变更
- **每日复盘**：自动检测当日笔记变更,AI 生成工作总结(每天晚上9点生成)
- **每周复盘**：汇总本周每日复盘,生成周度工作报告(每周五晚上6点生成)
- **变更可视化**：对比快照,展示新增/修改/删除的笔记
- **覆盖执行**：手动或自动触发都会覆盖当天已有的复盘结果
- **首次运行**：没有摘要时生成当前笔记仓库的概要,有快照时才开始生成复盘
- **启动补执行**：
  - 插件启动后延迟 10 秒检查
  - 如果今天复盘时间已过但未执行 → 补执行今天的复盘
  - 如果今天复盘时间未到 → 补执行昨天的复盘
  - 不会同时补执行两天

**每日复盘流程**：

```
手动触发 / 定时触发(每天 21:00)
    ↓
┌───────────────────────────────┐
│ 阶段1:检测首次运行            │
└───────────────────────────────┘
    ↓
- 检查是否存在笔记摘要和文件夹摘要
- 不存在 → 进入"首次运行"分支
    - 扫描所有笔记,生成仓库概要
    - 调用 AI 生成初始概要报告
    - 保存到 {配置的复盘目录}/daily/{date}.md
    - 创建初始快照
    - 跳过后续阶段,完成
- 存在 → 继续正常复盘流程
    ↓
┌───────────────────────────────┐
│ 阶段2:检测快照状态            │
└───────────────────────────────┘
    ↓
- 读取 data/cache/snapshots/snapshot-index.json
- 检测是否为首次运行(索引文件不存在)
    ↓
┌───────────────────────────────┐
│ 阶段3:变更检测                │
└───────────────────────────────┘
    ↓
- 读取 data/cache/snapshots/ 中的快照
- 使用 Vault.cachedRead() 高效读取笔记内容
- 扫描当前笔记(跳过 .obsidian/)
- 对比 hash,检测新增/修改/删除
- 计算 diff 内容(行级差异)
    ↓
┌───────────────────────────────┐
│ 阶段4:生成复盘                │
└───────────────────────────────┘
    ↓
- 根据变更内容构建 AI Prompt
- 调用 AI 生成复盘报告
- 保存到 {配置的复盘目录}/daily/{date}.md(覆盖当日已存在的文件)
    ↓
┌───────────────────────────────┐
│ 阶段5:更新快照                │
└───────────────────────────────┘
    ↓
- 对变更的笔记:
  - 计算新 hash
  - 压缩内容到 data/cache/snapshots/{hash}.gz
- 更新 data/cache/snapshots/snapshot-index.json
- 删除旧的快照文件
    ↓
完成
```

**每周复盘流程**：

```
手动触发 / 定时触发(每周五 18:00)
    ↓
┌───────────────────────────────┐
│ 阶段1:收集本周每日复盘        │
└───────────────────────────────┘
    ↓
- 读取 {配置的复盘目录}/daily/ 目录
- 筛选出本周的复盘文件(根据日期计算周次)
- 提取所有复盘内容
    ↓
┌───────────────────────────────┐
│ 阶段2:生成周复盘              │
└───────────────────────────────┘
    ↓
- 根据本周每日复盘构建 AI Prompt
- 调用 AI 生成周复盘报告
- 保存到 {配置的复盘目录}/weekly/{week}.md(覆盖当周已存在的文件)
    ↓
完成
```

---

### 仓库概要模板

首次运行复盘时，如果不存在笔记摘要和文件夹摘要，会生成仓库概要：

```markdown
# 仓库概要

> 生成时间：{date}
> 类型：首次运行概要

## 基本信息

- **笔记总数**：{count} 篇
- **文件夹总数**：{count} 个
- **主要文件夹**：列出一级文件夹
- **最近活跃**：最近 7 天修改的笔记数量

## 主要内容

{AI 生成的仓库主要内容描述，包括：
- 主要知识领域
- 核心主题标签
- 内容组织结构
- 使用建议}

## 文件夹结构

{列出主要文件夹及其用途}
```

---

### 3. 主题调研

**核心特性**：
- **用户身份识别**：分析笔记内容,推断职业身份和当前关注点
- **项目组成识别**：分析笔记库的构成(笔记/代码/资源)
- **智能主题推荐**：基于内容分析生成调研候选主题
- **知识点级去重**：避免重复调研相似主题
- **多类型调研报告**：4 种类型的调研报告

**调研类型**：

| 调研类型 | 英文标识 | 目的 | 适用场景 | 报告长度 |
|----------|----------|------|----------|----------|
| 前沿追踪 | trending | 深入了解前沿技术/趋势 | 用户新技术、新框架、新方法 | 1500-2500 字 |
| 问题解决 | problem-solving | 解决具体问题 | 用户笔记中的问题、TODO | 800-1500 字 |
| 深度学习 | deep-dive | 系统学习某主题 | 用户深入的技术领域 | 1000-2000 字 |
| 灵感启发 | inspiration | 激发新思路 | 用户模糊想法、探索方向 | 500-800 字 |

**调研流程**：

```
每日定时触发(每天 09:00) / 手动触发
    ↓
┌───────────────────────────────┐
│ 阶段0:生成笔记摘要（共享）     │
└───────────────────────────────┘
    ↓
- 调用 SummarizerService.run()
- 检查摘要缓存（contentHash）
- 只对新/变更文件调用 AI
- 返回: { new_summaries, updated_summaries, failed }
    ↓
┌───────────────────────────────┐
│ 阶段1:生成文件夹摘要          │
└───────────────────────────────┘
    ↓
- 调用 SummarizerService.generateFolderSummaries()
- 基于笔记摘要生成文件夹主题摘要
- 存储到 data/cache/folder_summaries/{hash}.json
    ↓
┌───────────────────────────────┐
│ 阶段2:身份识别(7天更新)      │
└───────────────────────────────┘
    ↓
- 读取 data/cache/identity/profile.json
- 检查是否需要更新(距上次超过7天)
- 如需要:调用 AI 分析笔记摘要，推断用户身份
    ↓
┌───────────────────────────────┐
│ 阶段3:主题生成                │
└───────────────────────────────┘
    ↓
- ResearchService 基于摘要+身份生成候选(8-10个)
- 每个主题标注类型(trending/problem-solving/deep-dive/inspiration)
- 保存到 data/cache/research/topics/{date}.json
    ↓
┌───────────────────────────────┐
│ 阶段4:去重过滤                │
└───────────────────────────────┘
    ↓
- 读取 data/cache/research/history/index.json
- 关键词重叠检查(计算 Jaccard 相似度)
- 对重叠率 > 40% 的主题,进行 AI 语义判断
- 时间衰减处理:
  - 7天内:强去重(重叠 > 30% 视为重复)
  - 7-14天:弱去重(重叠 > 60% 视为重复)
  - 14天以上:不参与去重
- 过滤并替换重复主题
    ↓
┌───────────────────────────────┐
│ 阶段5:主题排序筛选            │
└───────────────────────────────┘
    ↓
- AI 从候选中选出最佳 3 个主题
- 更新 data/cache/research/topics/{date}.json
    ↓
┌───────────────────────────────┐
│ 阶段6:生成调研报告            │
└───────────────────────────────┘
    ↓
- 遍历 3 个主题
- 根据类型选择对应模板
- 调用 AI 生成报告,支持 Obsidian 链接 [[笔记标题]]
- 保存到 {调研目录}/{date}-{slug}.md
    ↓
┌───────────────────────────────┐
│ 阶段7:更新历史索引            │
└───────────────────────────────┘
    ↓
- 从报告中提取 5-10 个核心知识点
- 更新 data/cache/research/history/index.json
- 清理过期数据(30天前的候选主题)
    ↓
完成
```

---

## 调研报告模板

### 1. 前沿追踪型 (trending)

**特点**：像一篇优质公众号文章,聚焦单一主题,深度介绍 + 真实案例
**报告长度**：1500-2500 字

```markdown
# {主题标题}

> 生成时间：{date}
> 调研类型：前沿追踪
> 使用模型：{model}

## 这是什么？

[用通俗易懂的语言解释这个技术/趋势是什么,解决什么问题]
[类比或比喻帮助理解]

## 为什么值得关注？

[说明这个技术的重要性和发展趋势]
[数据支撑：使用量、社区热度、大厂采用情况等]

## 核心原理

[深入但不晦涩地解释工作原理]
[配合示意图或代码片段]

### 关键概念 1
详细解释...

### 关键概念 2
详细解释...

## 真实案例

### 案例 1：{公司/项目名}
- **背景**：他们面临什么问题
- **方案**：如何使用这个技术
- **效果**：带来了什么改进

### 案例 2：{公司/项目名}
- **背景**：...
- **方案**：...
- **效果**：...

## 如何开始

[针对用户身份,给出具体的入门路径]

1. **第一步**：具体操作
2. **第二步**：具体操作
3. **第三步**：具体操作

## 注意事项

- 常见误区 1
- 常见误区 2
- 适用边界说明

## 延伸资源

- [官方文档](URL) - 一句话说明
- [推荐教程](URL) - 一句话说明
- [深度文章](URL) - 一句话说明
```

### 2. 问题解决型 (problem-solving)

**特点**：方案对比 + 推荐选择 + 实施步骤
**报告长度**：800-1500 字

```markdown
# {问题描述}

> 生成时间：{date}
> 调研类型：问题解决
> 使用模型：{model}

## 问题背景

[问题的上下文和约束条件]
[用户可能遇到的具体场景]

## 解决方案对比

| 方案 | 核心思路 | 优点 | 缺点 | 适用场景 |
|------|----------|------|------|----------|
| 方案 A | ... | ... | ... | ... |
| 方案 B | ... | ... | ... | ... |
| 方案 C | ... | ... | ... | ... |

## 推荐方案

**针对你的情况,推荐：{方案名}**

**理由**：
1. 理由 1
2. 理由 2
3. 理由 3

## 实施步骤

### 步骤 1：{标题}
具体操作说明...

### 步骤 2：{标题}
具体操作说明...

### 步骤 3：{标题}
具体操作说明...

## 可能遇到的问题

| 问题 | 解决方法 |
|------|----------|
| 问题 1 | 解决方法 |
| 问题 2 | 解决方法 |

## 参考资料

- [资源名](URL)
- [资源名](URL)
```

### 3. 深度学习型 (deep-dive)

**特点**：系统性知识梳理,从概念到实践
**报告长度**：1000-2000 字

```markdown
# {主题} - 深度解析

> 生成时间：{date}
> 调研类型：深度学习
> 使用模型：{model}

## 概述

[一段话说明这是什么,为什么重要,学完能做什么]

## 知识地图

```
{主题}
├── 基础概念
│   ├── 概念 A
│   └── 概念 B
├── 核心机制
│   ├── 机制 1
│   └── 机制 2
└── 实践应用
    ├── 场景 1
    └── 场景 2
```

## 核心概念

### 概念 A
详细解释...

**要点**：
- 要点 1
- 要点 2

### 概念 B
详细解释...

## 工作原理

[详细解释机制/流程,配合图示或代码]

## 实践指南

### 场景 1：{场景描述}

**步骤**：
1. ...
2. ...

**代码示例**：
```{language}
// 示例代码
```

### 场景 2：{场景描述}
...

## 常见误区

❌ **误区 1**：错误认知
✅ **正解**：正确理解

❌ **误区 2**：错误认知
✅ **正解**：正确理解

## 进阶路径

- **入门**：[资源](URL) - 说明
- **进阶**：[资源](URL) - 说明
- **精通**：[资源](URL) - 说明
```

### 4. 灵感启发型 (inspiration)

**特点**：轻量、发散、激发思考
**报告长度**：500-800 字

```markdown
# {主题} - 灵感碰撞

> 生成时间：{date}
> 调研类型：灵感启发
> 使用模型：{model}

## 💡 核心洞察

> {一句话核心观点,引发思考}

## 有趣的视角

### 视角 1：{标题}
{描述这个视角带来的启发}

### 视角 2：{标题}
{描述这个视角带来的启发}

## 相关案例

### {案例标题}
- **做了什么**：简述
- **有趣之处**：为什么值得关注
- **可借鉴点**：对你的启发

## 可能的应用方向

基于你当前的工作({currentFocus}),这个想法可能可以：

1. **方向 1**：简述可能性
2. **方向 2**：简述可能性
3. **方向 3**：简述可能性

## 延伸探索

- [相关资源](URL) - 一句话说明
- [相关资源](URL) - 一句话说明
```

---

## Obsidian 适配

### 文件操作适配

- 使用 `TFile` API 获取笔记元数据
- 使用 `Vault.read()` 读取笔记内容
- 使用 `Vault.cachedRead()` 高效读取笔记(优先使用缓存,仅用于快照)
- 使用 `Vault.create()` 创建新笔记
- 使用 `Vault.modify()` 修改笔记
- 使用 `Vault.rename()` 移动/重命名笔记
- 使用 `Vault.delete()` 删除笔记
- 使用 `MetadataCache.getFileCache()` 获取笔记元数据缓存(YAML Frontmatter、标签、链接等)

### 数据存储策略

**插件内缓存**：
- `.obsidian/plugins/ai-note/data/cache/` - 存储所有缓存数据
- 笔记摘要、文件夹摘要、快照、身份档案、候选主题、调研历史
- 这些数据不需要在笔记中可见,不参与 Obsidian 搜索

**笔记根目录可见**：
- 复盘报告 - 存储在配置的目录(默认"复盘/")
- 调研报告 - 存储在配置的目录(默认"调研/")
- 这些文件是 Markdown 格式,用户可以直接查看和编辑
- 自动添加到 Obsidian 搜索索引

### 路径配置

在插件设置中可配置：
- **复盘目录**：默认"复盘/",可自定义(如"AI Note/复盘/")
- **调研目录**：默认"调研/",可自定义(如"AI Note/调研/")
- **待整理目录**：默认"待整理/",不确定归档位置的笔记
- **隐藏目录过滤**：默认自动过滤 `.` 开头的目录，可添加其他目录到过滤列表

### 链接和嵌入支持

- 生成报告时使用 Obsidian 链接格式 `[[笔记标题]]`
- 支持嵌入格式 `![[图片.png]]` 或 `![[文档.md]]`
- 支持双向链接,调研/复盘报告可链接到源笔记
- 复盘和调研报告存储在笔记根目录,可正常使用 Obsidian 的链接和搜索功能

### 搜索集成

- 复盘和调研报告自动添加到 Obsidian 搜索索引
- 支持全文搜索和标签搜索
- 缓存数据不参与搜索

### 支持的笔记格式

- Markdown (.md) - 完全支持
- Canvas (.canvas) - 提取文本内容
- 图片/附件 - 仅记录元数据

---

## Obsidian 命令

| 命令 | 功能 |
|------|------|
| `AI Note: Execute Archive` | 执行智能归档 |
| `AI Note: Initialize Snapshot` | 初始化快照 |
| `AI Note: Generate Daily Review` | 生成每日复盘 |
| `AI Note: Generate Weekly Review` | 生成每周复盘 |
| `AI Note: Generate Research` | 生成调研报告 |
| `AI Note: Open Today's Review` | 打开今日复盘 |
| `AI Note: Open Today's Research` | 打开今日调研 |

---

## PathManager 服务

```typescript
// services/pathManager.ts
export class PathManager {
    private pluginDataPath: string;
    private vaultPath: string;
    private settings: {
        reviewsDir: string;      // 复盘目录配置,默认"复盘/"
        researchDir: string;     // 调研目录配置,默认"调研/"
        unsortedDir: string;     // 待整理目录配置,默认"待整理/"
        hiddenDirectories: string[];  // 隐藏目录过滤列表
    };

    constructor(pluginDataPath: string, vaultPath: string, settings: any) {
        this.pluginDataPath = pluginDataPath;
        this.vaultPath = vaultPath;
        this.settings = settings;
    }

    // ====== 插件数据目录（.obsidian/plugins/ai-note/data/）=======
    // 缓存目录
    get summariesDir(): string {
        return path.join(this.pluginDataPath, 'data', 'cache', 'summaries');
    }

    get folderSummariesDir(): string {
        return path.join(this.pluginDataPath, 'data', 'cache', 'folder_summaries');
    }

    get snapshotsDir(): string {
        return path.join(this.pluginDataPath, 'data', 'cache', 'snapshots');
    }

    get identityDir(): string {
        return path.join(this.pluginDataPath, 'data', 'cache', 'identity');
    }

    // 调研缓存目录
    get researchTopicsDir(): string {
        return path.join(this.pluginDataPath, 'data', 'cache', 'research', 'topics');
    }

    get researchHistoryDir(): string {
        return path.join(this.pluginDataPath, 'data', 'cache', 'research', 'history');
    }

    // ====== 笔记根目录（用户可见）=======
    // 复盘目录（用户可配置）
    get reviewsDir(): string {
        return path.join(this.vaultPath, this.settings.reviewsDir || '复盘');
    }

    get dailyReviewsDir(): string {
        return path.join(this.getReviewsDir(), 'daily');
    }

    get weeklyReviewsDir(): string {
        return path.join(this.getReviewsDir(), 'weekly');
    }

    // 调研报告目录（用户可配置）
    get researchReportsDir(): string {
        return path.join(this.vaultPath, this.settings.researchDir || '调研');
    }

    // 待整理目录（用户可配置）
    get unsortedDir(): string {
        return path.join(this.vaultPath, this.settings.unsortedDir || '待整理');
    }
}
```

---

## 需要修改的服务

| 服务 | 修改内容 |
|------|---------|
| `SummaryStore` | 使用 PathManager,适配 Obsidian TFile API |
| `FolderSummaryStore` | 使用 PathManager,适配 Obsidian API |
| `SnapshotService` | 使用 PathManager,使用 Vault.cachedRead 读取笔记(仅用于快照) |
| `ReviewService` | 使用 PathManager,笔记路径改为用户可配置的复盘目录 |
| `IdentityService` | 使用 PathManager |
| `TopicGeneratorService` | 使用 PathManager |
| `ResearchHistoryService` | 使用 PathManager |
| `ResearchService` | 使用 PathManager,支持 Obsidian 链接生成,报告路径改为用户可配置的调研目录 |
| `ContentAnalyzer` | 使用 PathManager,使用 MetadataCache 获取笔记元数据 |
| `FileScanner` | 使用 Vault.getMarkdownFiles() 获取笔记,跳过 .obsidian/ |
| `ArchiverService` | 使用 Vault.rename() 进行笔记移动,路径使用用户可配置的归档目录 |
| `PathManager` | 新增服务,统一管理所有路径 |

---

## 实现优先级

| 优先级 | 任务 | 复杂度 | 说明 |
|--------|------|--------|------|
| P0 | PathManager 服务 | 低 | 统一路径管理,支持配置路径 |
| P0 | 适配所有 Store 服务 | 中 | 使用 PathManager,适配新路径 |
| P0 | 适配 FileScanner 到 Vault API | 中 | 使用 getMarkdownFiles,跳过 .obsidian/ |
| P0 | 适配 ArchiverService 到 Vault.rename | 中 | 使用 Obsidian API 移动笔记 |
| P0 | 适配 SnapshotService 到 Vault.cachedRead | 中 | 高效读取笔记(仅用于快照) |
| P0 | 适配 ContentAnalyzer 到 MetadataCache | 中 | 获取笔记元数据(YAML、标签、链接) |
| P0 | 适配 ReviewService 支持 Obsidian 链接 | 低 | 生成报告时使用 `[[链接]]` 格式 |
| P0 | 修正智能归档流程 | 低 | 移除"处理最近笔记"阶段,只过滤14天内笔记 |
| P0 | 修正复盘流程 | 中 | 首次运行生成仓库概要,支持覆盖复盘结果 |
| P0 | 修正文件夹摘要增量更新 | 低 | sampleNotes 限制为10个 |

---

*本 PRD 整合了智能归档、每日复盘、主题调研三大核心功能,适配为 Obsidian 插件版本。*
